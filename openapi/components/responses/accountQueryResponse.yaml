description: |
  Response information from a account query call
oneOf:
- $ref: '../schemas/accountQuery/authorizedSuccess.yaml'
- $ref: '../schemas/error.yaml'
discriminator:
  propertyName: result
  mapping:
    authorizedSuccess: '../schemas/accountQuery/authorizedSuccess.yaml'
    failure: '../schemas/error.yaml'
