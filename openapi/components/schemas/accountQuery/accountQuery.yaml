type: object
properties:
  headerInfo:
    type: object
    $ref: '../headerInfo.yaml'
  pageSize:
    type: integer
    description: Maximum number of accounts to return.
    example: 50
  accountQueries:
    type: array
    items:
      $ref: './accountQuery`.yaml'

required:
- headerInfo
- pageSize
- accountQueries
