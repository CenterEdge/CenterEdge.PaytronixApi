type: object
properties:
  header:
    $ref: './header.yaml' 
  items:
    type: array
    description: List of individual check items.
    items: 
      $ref: './checkItem.yaml'
  totals:
    $ref: './totals.yaml'
    
required:
  - header
  - items
  - totals