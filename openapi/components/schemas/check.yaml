type: object
properties:
  header:
    description: The header to the check. Contains details about the check as as whole, such as check number, number of seats, time opened, etc.
    $ref: './header.yaml' 
  items:
    type: array
    description: List of individual check items.
    items: 
      $ref: './checkItem.yaml'
  totals:
    description: The totals at the end of the check.
    $ref: './totals.yaml'
    
required:
  - header
  - items
  - totals